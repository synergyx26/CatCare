Hi <%= @user.name %>,

Someone requested a password reset for your CatCare account.

Reset your password (expires in 6 hours):
<%= @reset_url %>

If you didn't request this, you can safely ignore this email — your password won't change.

— CatCare
