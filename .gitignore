# Node
web/node_modules/
catcare/web/node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code internal settings
.claude/

# Unrelated nested directory (separate project, not part of CatCare)
catcare/

# Frontend env — deployment-specific (LAN IP for self-hosted, regenerated
# fresh by scripts/deploy-catcare.sh each deploy); never a fixed committed value
web/.env.production

# Personal credentials/migration scratch notes — never commit, contains live
# plaintext DB passwords, JWT secrets, and API keys
CatCare Migration.txt

# Local-only ops script — references real Proxmox LXC IP/SSH user/key path/
# container names; repo is public, keep this off GitHub
db-pull.sh
